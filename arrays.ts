const arr: (boolean | string)[] = [true, false, 'bye'];

arr.push(true);
arr.push('hello');

const nestedArray: (string | number)[][] = [];

nestedArray.push(['hello']);
// nestedArray.push(true);
nestedArray.push([7]);
